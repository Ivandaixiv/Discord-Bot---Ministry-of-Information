# Discord Bot - Ministry of Defense

Commands to run if ssh key not found

eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_rsa
---
# Credit
## DiscordBotTemplate

A starting Node.JS template for your own discord bot (2023)
